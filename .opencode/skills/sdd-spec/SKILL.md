---
name: sdd-spec
description: Enforce mandatory SDD, validate MoSCoW scope, freeze MVP web+Docker stack. Use for PRD and new stories.
---

## What I do
- SDD is mandatory: explore -> propose -> spec <-> design -> tasks -> apply -> verify -> archive. No improvisation.
- Read doc/proposal.md, freeze MUST scope.
- MVP is final: Gradio web UI + local Docker Compose (app + postgres).
- Future options stay valid but out of MVP: v0, Lovable, Supabase, Vercel, cloud deploy, AI summary. Park them to SHOULD/COULD/WONT, do not delete.
- Reject scope creep into MUST.

## When to use me
When defining or changing requirements.
