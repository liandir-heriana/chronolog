# ChronoLog local environment template (TSK-012).
# Copy to `.env` (git-ignored, never commit) and adjust: `cp .env.example .env`
# Placeholders only — no real secrets in this file.
POSTGRES_USER=chronolog
POSTGRES_PASSWORD=changeme
POSTGRES_DB=chronolog

# Assembled by docker-compose.yml at runtime; override only if needed:
# DATABASE_URL=postgresql://chronolog:changeme@postgres:5432/chronolog
